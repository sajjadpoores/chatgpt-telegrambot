version: '3.8'
services:
  ai-chat-bot:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    ports:
      - 3000:3000
    networks:
      - poores
    environment:

networks:
  poores:
    external: true
